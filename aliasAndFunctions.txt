# User specific aliases and functions
alias D2LOD='wine "/home/marcospaulo/.wine/drive_c/Program Files (x86)/Diablo II/Game.exe" -w -nofixaspect' # Para jogar o jogo Diablo 2
alias D2='firejail --noprofile wine "/home/marcospaulo/.wine/drive_c/Program Files (x86)/Diablo II/Game.exe" -w -nofixaspect' # Para jogar o jogo Diablo 2 PARA BATTLE NET ???
alias D2CINE='wine "/home/marcospaulo/.wine/drive_c/Program Files (x86)/Diablo II/Game.exe"' # Para Assistir os videos de Diablo 2
alias D3='firejail --noprofile wine /home/marcospaulo/.wine/drive_c/Program\ Files\ \(x86\)/Diablo\ III/Diablo\ III.exe' # Para jogar o jogo Diablo 3


# Avoid duplicates
export HISTCONTROL=ignoredups:erasedups
# When the shell exits, append to the history file instead of overwriting it
shopt -s histappend

# After each command, append to the history file and reread it
export PROMPT_COMMAND="${PROMPT_COMMAND:+$PROMPT_COMMAND$'\n'}history -a; history -c; history -r"

PATH=/bin:/home/marcospaulo/.local/bin:/home/marcospaulo/bin:/home/marcospaulo/.local/bin:/home/marcospaulo/bin:/usr/share/Modules/bin:/usr/local/bin:/usr/local/sbin:/usr/bin:/usr/sbin:/usr/local/google-cloud-sdk/bin

#export WORKON_HOME=/home/marcospaulo/Dropbox/Python/.virtualenvs
#export PROJECT_HOME=/home/marcospaulo/Dropbox/Python/Devel
#source /home/marcospaulo/Dropbox/Python/virtualenvwrapper.sh

alias toCla="cat playedMP3.tmp ClassificandoPlayedMP3.txt | sort -u > /tmp/toOrderMP3Classificados.tmp; cp /tmp/toOrderMP3Classificados.tmp ClassificandoPlayedMP3.txt;"
alias toIns="cp toPlayMP3.txt toPlayMP3.bkp; cp toPlayMP3Instrumentais.txt toPlayMP3.txt;"

#naoEstaPronto# export PROMPT_COMMAND='echo -n "pts/" > /tmp/teste; echo $(tty | awk -F"/" "{print $4}") >> /tmp/teste; echo -en "\033]0;$(whoami)@$(hostname)|$(pwd|awk -F'/' '{print $NF}')|$(ps aux | grep "$(cat /tmp/teste)" | egrep -v 'bash|grep|aux' | tr -s ' ' | awk -F' ' '{print $NF}' | head -n1)\a"'

numCla(){
	cat ClassificandoPlayedMP3.txt | awk -F' ' '{print $2}' | grep ^[a-Z] | sort | uniq -c;
}

work(){
	okular Downloads/Apress\ PHP\ Objects\ Patterns\ And\ Practice\ \(2008\)\ Matt\ Zandstra.pdf & firefox & nautilus & sublime;
}
